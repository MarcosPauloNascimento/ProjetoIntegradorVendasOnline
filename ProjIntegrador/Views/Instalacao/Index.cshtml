@model IEnumerable<ProjIntegrador.Models.Instalacao>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Endereco.Descricao)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Instalador.InstaladorId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Venda.VendaId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DataInstalacao)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Observacao)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Endereco.Descricao)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Instalador.InstaladorId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Venda.VendaId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DataInstalacao)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Observacao)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.InstalacaoId }) |
            @Html.ActionLink("Details", "Details", new { id=item.InstalacaoId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.InstalacaoId })
        </td>
    </tr>
}

</table>
